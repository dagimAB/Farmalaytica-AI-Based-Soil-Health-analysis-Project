// secrets.h.example
// Copy this file to `../include/secrets.h` and replace placeholders with your real values.
// DO NOT commit real secrets to version control.

#pragma once

#define WIFI_SSID "YOUR_WIFI_SSID"
#define WIFI_PASSWORD "YOUR_WIFI_PASSWORD"
// API endpoint where the firmware sends NPK readings (update as needed):
#define API_URL "http://your-api:3000/api/predict"