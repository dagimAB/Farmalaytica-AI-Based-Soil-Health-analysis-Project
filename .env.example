# Example environment variables for the Farmalytica web app
# Copy to .env.local and fill with your secrets (DO NOT commit .env.local)

# MongoDB connection string
MONGODB_URI="mongodb+srv://<user>:<password>@cluster.example.mongodb.net/farmalytica?retryWrites=true&w=majority"

# OpenWeather API key
OPENWEATHER_API_KEY="your_openweather_api_key"

# Optional: path to your Python executable for local ML bridge
PYTHON_PATH="python"